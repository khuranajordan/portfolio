---
phase: 04-hero-section
plan: 02
type: execute
domain: ui-design
---

<objective>
Add entrance animations and visual effects to hero section using Framer Motion.

Purpose: Enhance hero section with smooth animations that create memorable first impression and demonstrate attention to detail.
Output: Animated hero section with staggered text reveals, floating elements, and smooth transitions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@.planning/phases/02-theme-system/02-01-SUMMARY.md
@.planning/phases/02-theme-system/02-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-01-SUMMARY.md
@.planning/phases/03-layout-structure/03-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-03-SUMMARY.md
@.planning/phases/04-hero-section/04-01-SUMMARY.md
@src/components/hero.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Framer Motion and create animated background</name>
  <files>package.json, src/components/hero.tsx</files>
  <action>
Install Framer Motion and add animated background elements to hero section.

Install Framer Motion:
Run: npm install framer-motion

Update src/components/hero.tsx to add animated background:
```tsx
import { Button } from '@/components/ui/button';
import { ArrowDown } from 'lucide-react';
import { motion } from 'framer-motion';

export function Hero() {
  const scrollToProjects = () => {
    const element = document.getElementById('projects');
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  };

  return (
    <section
      id="hero"
      className="min-h-screen flex items-center justify-center relative overflow-hidden"
    >
      {/* Animated background gradient */}
      <motion.div
        className="absolute inset-0 bg-gradient-to-br from-primary/5 via-background to-background -z-10"
        animate={{
          backgroundPosition: ['0% 0%', '100% 100%'],
        }}
        transition={{
          duration: 20,
          repeat: Infinity,
          repeatType: 'reverse',
        }}
        style={{
          backgroundSize: '200% 200%',
        }}
      />

      {/* Floating orbs */}
      <motion.div
        className="absolute top-20 left-10 w-72 h-72 bg-primary/10 rounded-full blur-3xl -z-10"
        animate={{
          y: [0, -30, 0],
          x: [0, 20, 0],
        }}
        transition={{
          duration: 8,
          repeat: Infinity,
          ease: 'easeInOut',
        }}
      />
      <motion.div
        className="absolute bottom-20 right-10 w-96 h-96 bg-primary/5 rounded-full blur-3xl -z-10"
        animate={{
          y: [0, 30, 0],
          x: [0, -20, 0],
        }}
        transition={{
          duration: 10,
          repeat: Infinity,
          ease: 'easeInOut',
        }}
      />

      <div className="container mx-auto px-4 py-20 md:py-0">
        <div className="max-w-4xl mx-auto text-center space-y-6 md:space-y-8">
          {/* Greeting */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="space-y-2 md:space-y-4"
          >
            <p className="text-base md:text-lg text-primary font-medium">
              Hi, I'm
            </p>

            {/* Name with gradient effect */}
            <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight">
              <span className="text-primary">Rohan</span> Patel
            </h1>

            {/* Title */}
            <h2 className="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-semibold text-muted-foreground">
              Senior Frontend Developer
            </h2>
          </motion.div>

          {/* Value Proposition */}
          <motion.p
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.2 }}
            className="text-base sm:text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto leading-relaxed px-4"
          >
            I build exceptional digital experiences with React, TypeScript, and modern web technologies.
            Transforming ideas into polished, performant applications.
          </motion.p>

          {/* CTA Buttons */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.4 }}
            className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center pt-4 px-4"
          >
            <Button
              size="lg"
              onClick={scrollToProjects}
              className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto"
            >
              View My Work
            </Button>
            <Button
              size="lg"
              variant="outline"
              onClick={() => {
                const element = document.getElementById('contact');
                if (element) {
                  element.scrollIntoView({ behavior: 'smooth' });
                }
              }}
              className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto"
            >
              Get In Touch
            </Button>
          </motion.div>

          {/* Scroll Indicator */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ duration: 0.6, delay: 0.6 }}
            className="pt-8 md:pt-12"
          >
            <Button
              variant="ghost"
              size="icon"
              onClick={() => {
                const element = document.getElementById('about');
                if (element) {
                  element.scrollIntoView({ behavior: 'smooth' });
                }
              }}
              aria-label="Scroll to about section"
              className="h-10 w-10 animate-bounce"
            >
              <ArrowDown className="h-5 w-5 md:h-6 md:w-6" />
            </Button>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

Animations added:
- Gradient background with slow pan (20s duration)
- Floating orbs with smooth movement (8s and 10s durations)
- Staggered fade-in for content blocks (0, 0.2s, 0.4s, 0.6s delays)
- Upward motion (y: 20 → 0) for entrance
- Smooth easing (easeInOut)
  </action>
  <verify>
- framer-motion in package.json
- Background gradient animates smoothly
- Floating orbs visible and moving
- Content fades in with staggered delays
- No performance issues (60fps)
- Works in both themes
  </verify>
  <done>
Animated background and entrance animations implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement text entrance animations</name>
  <files>src/components/hero.tsx</files>
  <action>
Add sophisticated text reveal animations with word-by-word or character-level effects.

Update src/components/hero.tsx with enhanced text animations:
Replace the greeting/title section with animated text reveals:

```tsx
{/* Inside the motion.div for greeting section */}

<p className="text-base md:text-lg text-primary font-medium">
  Hi, I'm
</p>

{/* Name with letter-by-letter animation */}
<h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight">
  <motion.span
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ duration: 0.8 }}
    className="text-primary inline-block"
  >
    Rohan
  </motion.span>{' '}
  <motion.span
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ duration: 0.8, delay: 0.3 }}
    className="inline-block"
  >
    Patel
  </motion.span>
</h1>

{/* Title with word reveal */}
<h2 className="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-semibold text-muted-foreground">
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5 }}
    className="inline-block"
  >
    Senior
  </motion.span>{' '}
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5, delay: 0.1 }}
    className="inline-block"
  >
    Frontend
  </motion.span>{' '}
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5, delay: 0.2 }}
    className="inline-block"
  >
    Developer
  </motion.span>
</h2>
```

Also enhance button animations with hover effects:
```tsx
{/* CTA Buttons with hover animations */}
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: 0.4 }}
  className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center pt-4 px-4"
>
  <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
    <Button
      size="lg"
      onClick={scrollToProjects}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto"
    >
      View My Work
    </Button>
  </motion.div>
  <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
    <Button
      size="lg"
      variant="outline"
      onClick={() => {
        const element = document.getElementById('contact');
        if (element) {
          element.scrollIntoView({ behavior: 'smooth' });
        }
      }}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto"
    >
      Get In Touch
    </Button>
  </motion.div>
</motion.div>
```

Animations:
- Name letters fade in sequentially (0.3s delay)
- Title words reveal with upward motion
- Buttons scale on hover (1.05x)
- Buttons tap effect (0.95x)
- All smooth and performant
  </action>
  <verify>
- Name animates letter by letter
- Title words animate sequentially
- Button hover effects smooth
- No janky animations
- All animations respect prefers-reduced-motion (will address if issues)
- Works in both themes
  </verify>
  <done>
Text entrance animations implemented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Hero section with complete animations: animated gradient background, floating orbs, staggered content reveals, text animations, and interactive button effects.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173

    On page load (fresh with Cmd+Shift+R):
    - Watch background gradient slowly pan (20s cycle)
    - Verify floating orbs move smoothly
    - Watch greeting fade in first
    - Watch name letters animate (Rohan → Patel with delay)
    - Watch title words reveal upward
    - Watch description fade in
    - Watch buttons slide up
    - Watch scroll indicator fade in last

    Interactions:
    - Hover over "View My Work" button - verify scale effect
    - Hover over "Get In Touch" button - verify scale effect
    - Click buttons - verify tap animation
    - Click scroll indicator - verify smooth scroll

    Performance:
    - Animations should be smooth (60fps)
    - No lag or stuttering
    - No console errors

    Theme test:
    - Toggle to dark mode
    - Verify all animations still work
    - Verify colors adapt correctly
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Framer Motion installed
- [ ] Background animations working
- [ ] Text entrance animations implemented
- [ ] Button hover effects working
- [ ] Animations smooth and performant
- [ ] Works in both themes
</verification>

<success_criteria>
- All tasks completed
- Hero section fully animated
- Smooth entrance effects
- Interactive hover states
- Performance acceptable
- Ready for CTA enhancements
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-section/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Add Entrance Animations Summary

**Hero section enhanced with Framer Motion animations: gradient background, floating orbs, staggered reveals, and interactive effects**

## Accomplishments
- Framer Motion integrated
- Animated gradient background (20s cycle)
- Two floating orbs with smooth motion
- Staggered content entrance (0, 0.2s, 0.4s, 0.6s)
- Name letter-by-letter animation
- Title word-by-word reveal
- Button hover and tap effects
- Performance optimized (60fps)

## Files Created/Modified
- `package.json` - Added framer-motion
- `src/components/hero.tsx` - All animations implemented

## Decisions Made
- Framer Motion over React Spring (better API, more popular)
- Staggered delays (0.1-0.2s) for sequential reveals
- Subtle orb animations (8-10s duration)
- Button scale effects (1.05x hover, 0.95x tap)
- Smooth easing throughout (easeInOut)
- Background gradient pan (slow, subtle)

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
Ready for 04-03-PLAN.md (Implement CTA buttons)
</output>
