---
phase: 04-hero-section
plan: 03
type: execute
domain: ui-design
---

<objective>
Implement polished call-to-action buttons with enhanced hover effects and micro-interactions.

Purpose: Complete hero section with production-ready CTA buttons that invite engagement and demonstrate attention to detail.
Output: Fully functional CTA buttons with gradient effects, icon integration, and smooth hover animations.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@.planning/phases/02-theme-system/02-01-SUMMARY.md
@.planning/phases/02-theme-system/02-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-01-SUMMARY.md
@.planning/phases/03-layout-structure/03-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-03-SUMMARY.md
@.planning/phases/04-hero-section/04-01-SUMMARY.md
@.planning/phases/04-hero-section/04-02-SUMMARY.md
@src/components/hero.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enhanced CTA buttons with icons</name>
  <files>src/components/hero.tsx</files>
  <action>
Enhance CTA buttons with icons, gradients, and improved visual hierarchy.

Update src/components/hero.tsx CTA buttons section:
```tsx
{/* Import icons */}
import { ArrowDown, FolderOpen, Mail } from 'lucide-react';

{/* Inside the motion.div for CTA buttons section */}

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: 0.4 }}
  className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center pt-4 px-4"
>
  <motion.div
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
  >
    <Button
      size="lg"
      onClick={scrollToProjects}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto group relative overflow-hidden"
    >
      <span className="relative z-10 flex items-center gap-2">
        <FolderOpen className="w-5 h-5" />
        View My Work
      </span>
      <div className="absolute inset-0 bg-gradient-to-r from-primary/0 via-primary/20 to-primary/0 translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700" />
    </Button>
  </motion.div>

  <motion.div
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
  >
    <Button
      size="lg"
      variant="outline"
      onClick={() => {
        const element = document.getElementById('contact');
        if (element) {
          element.scrollIntoView({ behavior: 'smooth' });
        }
      }}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto group relative"
    >
      <span className="flex items-center gap-2">
        <Mail className="w-5 h-5" />
        Get In Touch
      </span>
    </Button>
  </motion.div>
</motion.div>
```

Enhancements:
- Icons added to both buttons (FolderOpen, Mail)
- Shimmer effect on primary button (gradient sweep)
- Icon + text layout for clarity
- Flex layout for alignment
- Relative positioning for shimmer effect
- Smooth gradient transition (700ms)
  </action>
  <verify>
- Icons visible on both buttons
- Shimmer effect on hover (primary button)
- Icons aligned with text
- Button sizes consistent
- No spacing issues
- Works in both themes
  </verify>
  <done>
Enhanced CTA buttons with icons and effects
  </done>
</task>

<task type="auto">
  <name>Task 2: Add micro-interactions and accessibility improvements</name>
  <files>src/components/hero.tsx</files>
  <action>
Add focus states, keyboard navigation, and ripple effects for production-ready buttons.

Update src/components/hero.tsx with accessibility improvements:
```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: 0.4 }}
  className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center pt-4 px-4"
>
  <motion.div
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
  >
    <Button
      size="lg"
      onClick={scrollToProjects}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto group relative overflow-hidden focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
    >
      <span className="relative z-10 flex items-center gap-2 group-hover:gap-3 transition-all duration-300">
        <FolderOpen className="w-5 h-5 transition-transform group-hover:rotate-12" />
        View My Work
        <ArrowDown className="w-4 h-4 opacity-0 -translate-x-2 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300" />
      </span>
      <div className="absolute inset-0 bg-gradient-to-r from-primary/0 via-primary/20 to-primary/0 translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700" />
    </Button>
  </motion.div>

  <motion.div
    whileHover={{ scale: 1.05 }}
    whileTap={{ scale: 0.95 }}
  >
    <Button
      size="lg"
      variant="outline"
      onClick={() => {
        const element = document.getElementById('contact');
        if (element) {
          element.scrollIntoView({ behavior: 'smooth' });
        }
      }}
      className="text-base sm:text-lg px-6 sm:px-8 py-5 sm:py-6 w-full sm:w-auto group focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"
    >
      <span className="flex items-center gap-2 group-hover:gap-3 transition-all duration-300">
        <Mail className="w-5 h-5 transition-transform group-hover:scale-110" />
        Get In Touch
      </span>
    </Button>
  </motion.div>
</motion.div>
```

Accessibility and interaction improvements:
- focus-visible:ring for keyboard navigation
- ring-offset-2 for focus ring spacing
- Icon micro-animations (rotate, scale)
- Arrow icon appears on hover (primary button)
- Gap increases on hover (better spacing)
- All transitions smooth (300ms)
  </action>
  <verify>
- Tab to buttons - verify focus ring visible
- Press Enter/Space - verify buttons work
- Hover primary button - verify arrow icon appears
- Hover icons - verify micro-animations
- Focus rings properly offset from button
- All transitions smooth
  </verify>
  <done>
CTTA buttons production-ready with full accessibility
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CTA buttons enhanced with icons
- [ ] Shimmer effect working
- [ ] Focus states implemented
- [ ] Keyboard navigation working
- [ ] Icon micro-animations functional
- [ ] Accessibility verified
</verification>

<success_criteria>
- All tasks completed
- CTA buttons polished and production-ready
- Full accessibility compliance
- Smooth micro-interactions
- **Phase 4 complete** - ready for Phase 5 (Experience Timeline)
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-section/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Implement CTA Buttons Summary

**Production-ready CTA buttons with icons, shimmer effects, micro-interactions, and full accessibility**

## Accomplishments
- Icons integrated into buttons (FolderOpen, Mail, ArrowDown)
- Shimmer gradient effect on primary button
- Icon micro-animations (rotate, scale)
- Arrow icon reveal on hover
- Focus-visible rings for keyboard navigation
- Smooth spacing transitions
- Full WCAG compliance

## Files Created/Modified
- `src/components/hero.tsx` - CTA button enhancements

## Decisions Made
- Icons added for clarity and visual appeal
- Shimmer effect on primary only (hierarchy)
- Arrow reveal adds playfulness
- Focus-visible instead of focus (only visible on keyboard nav)
- Ring offset for better spacing
- 300ms transitions (fast but smooth)

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
**Phase 4 complete** - ready for Phase 5 (Experience Timeline)
</output>
