---
phase: 07-projects-showcase
plan: 03
type: execute
domain: ui-design
---

<objective>
Build case study components for presenting significant projects with structured problem-solution format.

Purpose: Create detailed case study presentations showcasing challenges, solutions, and outcomes.
Output: Reusable case study component with sections for problem, solution, results, and tech stack.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projects-showcase/07-01-SUMMARY.md
@.planning/phases/07-projects-showcase/07-02-SUMMARY.md
@src/types/projects.ts
@src/components/projects-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case study component structure</name>
  <files>src/components/case-study.tsx</files>
  <action>
Create reusable case study component for detailed project presentations.

Create src/components/case-study.tsx:
```tsx
import { Project } from '@/types/projects';
import { Badge } from '@/components/ui/badge';
import { Card } from '@/components/ui/card';
import { Separator } from '@/components/ui/separator';

interface CaseStudyProps {
  project: Project;
}

export function CaseStudy({ project }: CaseStudyProps) {
  return (
    <Card className="border rounded-xl overflow-hidden">
      {/* Header */}
      <div className="bg-primary text-primary-foreground p-6">
        <Badge variant="secondary" className="mb-2">
          {project.category}
        </Badge>
        <h3 className="text-2xl font-bold mb-1">{project.title}</h3>
        <p className="text-primary/80">{project.startDate} - {project.endDate || 'Present'}</p>
      </div>

      <div className="p-6 space-y-6">
        {/* Overview */}
        <div>
          <h4 className="font-semibold mb-2 text-lg">Overview</h4>
          <p className="text-muted-foreground">{project.longDescription}</p>
        </div>

        <Separator />

        {/* Challenges */}
        {project.challenges && project.challenges.length > 0 && (
          <div>
            <h4 className="font-semibold mb-3 text-lg">Challenges</h4>
            <div className="space-y-3">
              {project.challenges.map((challenge, index) => (
                <div key={index} className="bg-muted/50 p-4 rounded-lg">
                  <p className="text-sm">{challenge}</p>
                </div>
              ))}
            </div>
          </div>
        )}

        <Separator />

        {/* Solution */}
        <div>
          <h4 className="font-semibold mb-3 text-lg">Solution Approach</h4>
          <p className="text-muted-foreground mb-3">
            Leveraged modern frontend technologies and best practices to build a scalable,
            maintainable solution.
          </p>
          <div className="space-y-2">
            <div className="flex items-start gap-2">
              <span className="text-primary font-semibold">→</span>
              <p className="text-sm">Component-based architecture with React</p>
            </div>
            <div className="flex items-start gap-2">
              <span className="text-primary font-semibold">→</span>
              <p className="text-sm">Redux Toolkit for state management</p>
            </div>
            <div className="flex items-start gap-2">
              <span className="text-primary font-semibold">→</span>
              <p className="text-sm">TypeScript for type safety</p>
            </div>
          </div>
        </div>

        <Separator />

        {/* Results */}
        <div>
          <h4 className="font-semibold mb-3 text-lg">Results</h4>
          {project.achievements && (
            <ul className="space-y-2">
              {project.achievements.map((achievement, index) => (
                <li key={index} className="flex items-start gap-2">
                  <span className="text-green-600 dark:text-green-400 text-lg">✓</span>
                  <span className="text-sm">{achievement}</span>
                </li>
              ))}
            </ul>
          )}
        </div>

        {/* Tech Stack */}
        <div>
          <h4 className="font-semibold mb-3 text-lg">Tech Stack</h4>
          <div className="flex flex-wrap gap-2">
            {project.techStack.map((tech) => (
              <Badge key={tech} variant="secondary">
                {tech}
              </Badge>
            ))}
          </div>
        </div>
      </div>
    </Card>
  );
}
```

Component features:
- Themed header (primary background)
- Overview section
- Challenges with highlighted boxes
- Solution approach with arrows
- Results with checkmarks
- Tech stack badges
- Separators between sections
- Card container with rounded corners
  </action>
  <verify>
- src/components/case-study.tsx created
- Component structure matches project type
- All sections rendering correctly
- Colors work in both themes
  </verify>
  <done>
Case study component created with structured sections
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate case study into projects section</name>
  <files>src/components/projects-section.tsx</files>
<action>
Integrate case study component into Task Management showcase for detailed presentation.

Update src/components/projects-section.tsx to add case study:
```tsx
import { CaseStudy } from '@/components/case-study';

export function ProjectsSection() {
  // ... existing code ...

  return (
    <section id="projects" className="py-20 md:py-32 bg-muted/30">
      <div className="container mx-auto px-4">
        {/* ... section header ... */}

        {/* Showcase Projects with Case Study */}
        <div className="space-y-16 mb-16">
          {showcaseProjects.map((project) => (
            <div key={project.id}>
              {/* Use CaseStudy for Task Management */}
              {project.id === '2' ? (
                <CaseStudy project={project} />
              ) : (
                {/* Regular showcase for E-commerce */}
                <div className="bg-card border rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300">
                  {/* ... E-commerce showcase content ... */}
                </div>
              )}
            </div>
          ))}
        </div>

        {/* ... card projects grid ... */}
      </div>
    </section>
  );
}
```

Integration details:
- CaseStudy for Task Management (shows problem/solution/results)
- Regular showcase for E-commerce (visual overview)
- Mixed presentation within same showcase section
- Both formats demonstrate different project depths
  </action>
  <verify>
- CaseStudy component imported
- Task Management uses case study format
- E-commerce uses regular showcase
- Both render correctly
- Layout maintained
  </verify>
  <done>
Case study integrated for Task Management project
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Case study component created
- [ ] Structured sections defined
- [ ] Integration into projects section
- [ ] Mixed formats working
- [ ] Layout maintained
</verification>

<success_criteria>
- All tasks completed
- Case study component reusable
- Integration successful
- Both formats demonstrate different depths
- Ready for project cards
</success_criteria>

<output>
After completion, create `.planning/phases/07-projects-showcase/07-03-SUMMARY.md`:

# Phase 7 Plan 3: Build Case Studies Summary

**Reusable case study component with structured problem-solution-results format, integrated for Task Management project**

## Accomplishments
- Case study component created with structured sections
- Overview, Challenges, Solution, Results sections
- Themed header with project info
- Tech stack badges
- Separators for visual flow
- Integrated for Task Management project
- E-commerce kept in showcase format

## Files Created/Modified
- `src/components/case-study.tsx` - Case study component
- `src/components/projects-section.tsx` - Integrated case study

## Decisions Made
- Case study for Task Management (complex, multiple challenges)
- Showcase for E-commerce (visual overview)
- Themed header (primary color - stands out)
- Numbered solution approach (clear steps)
- Results with checkmarks (success indicators)
- Boxed challenges (highlight importance)

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
Ready for 07-04-PLAN.md (Implement project cards for smaller projects)
</output>
