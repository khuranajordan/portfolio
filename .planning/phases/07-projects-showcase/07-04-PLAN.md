---
phase: 07-projects-showcase
plan: 04
type: execute
domain: ui-design
---

<objective>
Implement project cards for smaller projects with hover effects and consistent styling.

Purpose: Create compact yet informative cards for Weather, Expense Tracker, and Blog applications.
Output: Polished project cards with hover effects, links, and responsive grid layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shic-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-projects-showcase/07-01-SUMMARY.md
@.planning/phases/07-projects-showcase/07-02-SUMMARY.md
@.planning/phases/07-projects-showcase/07-03-SUMMARY.md
@src/types/projects.ts
@src/components/projects-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enhanced project cards component</name>
  <files>src/components/project-card.tsx</files>
<action>
Create reusable project card component with enhanced design and interactions.

Create src/components/project-card.tsx:
```tsx
import { Project } from '@/types/projects';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Github, ExternalLink } from 'lucide-react';
import { motion } from 'framer-motion';

interface ProjectCardProps {
  project: Project;
}

export function ProjectCard({ project }: ProjectCardProps) {
  return (
    <motion.div
      layout
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      whileHover={{ y: -5 }}
      transition={{ duration: 0.3 }}
      className="bg-card border rounded-xl p-6 hover:shadow-xl hover:border-primary/50 transition-all duration-300 group h-full flex flex-col"
    >
      {/* Card Header */}
      <div className="mb-4">
        <Badge variant="secondary" className="mb-3">
          {project.category}
        </Badge>
        <h4 className="text-xl font-bold mb-2 group-hover:text-primary transition-colors">
          {project.title}
        </h4>
        <p className="text-sm text-muted-foreground line-clamp-3">
          {project.description}
        </p>
      </div>

      {/* Tech Stack - Show all on hover, preview default */}
      <div className="mb-4 flex-1">
        <p className="text-xs font-semibold text-muted-foreground mb-2">
          Tech Stack
        </p>
        <div className="flex flex-wrap gap-1.5">
          {project.techStack.map((tech) => (
            <Badge
              key={tech}
              variant="outline"
              className="text-xs"
            >
              {tech}
            </Badge>
          ))}
        </div>
      </div>

      {/* Meta Info */}
      <div className="text-xs text-muted-foreground mb-4 space-y-1">
        <p>📅 {project.startDate} - {project.endDate || 'Present'}</p>
        <p>📊 {project.status}</p>
      </div>

      {/* Actions */}
      <div className="flex gap-2 pt-2 mt-auto">
        {project.githubUrl && (
          <Button
            size="sm"
            variant="outline"
            className="flex-1"
            asChild
          >
            <a
              href={project.githubUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="w-full"
            >
              <Github className="w-4 h-4 mr-2" />
              Code
            </a>
          </Button>
        )}
        {project.liveUrl && (
          <Button
            size="sm"
            className="flex-1"
            asChild
          >
            <a
              href={project.liveUrl}
              target="_blank"
              rel="noopener noreferrer"
              className="w-full"
            >
              <ExternalLink className="w-4 h-4 mr-2" />
              Live
            </a>
          </Button>
        )}
      </div>
    </motion.div>
  );
}
```

Component features:
- Lift effect on hover (y: -5)
- Hover color change on title
- Tech stack badges (all shown)
- Line-clamp for description (3 lines)
- Status and dates displayed
- GitHub and Live demo buttons
- Flex layout for buttons
- Equal height cards (flex-col)
- Motion animations
  </action>
  <verify>
- src/components/project-card.tsx created
- Card layout balanced
- Hover effects working
- Buttons functional
- Responsive sizing
- Line-clamp working
  </verify>
  <done>
Enhanced project card component created
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace card sections with project card component</name>
<files>src/components/projects-section.tsx</files>
<action>
Update projects section to use new ProjectCard component for consistency.

Update src/components/projects-section.tsx card projects section:
```tsx
import { ProjectCard } from '@/components/project-card';

export function ProjectsSection() {
  // ... existing code ...

  return (
    <section id="projects" className="py-20 md:py-32 bg-muted/30">
      <div className="container mx-auto px-4">
        {/* ... section header ... */}
        {/* ... showcase projects ... */}

        {/* Card Projects - Grid Layout with ProjectCard */}
        <div>
          <h3 className="text-2xl font-bold mb-8">Other Projects</h3>
          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {cardProjects.map((project) => (
              <ProjectCard key={project.id} project={project} />
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}
```

Integration:
- Import ProjectCard component
- Replace inline card markup with component
- Remove card div (component handles everything)
- Maintain grid layout
- All props passed correctly
- Consistent styling across cards
  </action>
  <verify>
- ProjectCard component imported
- Cards use new component
- All card props passed
- Layout maintained
- Consistent styling
- Grid working
  </verify>
  <done>
Project cards replaced with component
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ProjectCard component created
- [ ] Card layout improved
- [ ] Hover effects enhanced
- [ ] Component integrated
- [ ] All cards consistent
</verification>

<success_criteria>
- All tasks completed
- Component reusable
- Cards visually polished
- Hover effects smooth
- Ready for tech badges and links
</success_criteria>

<output>
After completion, create `.planning/phases/07-projects-showcase/07-04-SUMMARY.md`:

# Phase 7 Plan 4: Implement Project Cards Summary

**Reusable project card component with enhanced hover effects and consistent styling**

## Accomplishments
- ProjectCard component created
- Lift effect on hover (y: -5)
- All tech stack badges visible
- Line-clamp for descriptions
- GitHub and Live demo buttons
- Equal height cards with flex layout
- Motion animations
- Integrated into projects section

## Files Created/Modified
- `src/components/project-card.tsx` - Project card component
- `src/components/projects-section.tsx` - Replaced cards with component

## Decisions Made
- Reusable component (consistency)
- All badges shown (no "+X" limitation)
- Line-clamp for descriptions (prevents overflow)
- Equal heights (flex-col)
- GitHub primary CTA for cards (code access)
- Live demo button (if available)
- Lift effect matches hero section

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
Ready for 07-05-PLAN.md (Add tech stack badges and external links)
</output>
