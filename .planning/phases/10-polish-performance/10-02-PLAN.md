---
phase: 10-polish-performance
plan: 02
type: execute
domain: ui-design
---

<objective>
Optimize responsive design across mobile, tablet, and desktop breakpoints for seamless experience on all devices.

Purpose: Ensure perfect layout, typography, and spacing at every breakpoint with mobile-first approach.
Output: Fully responsive site with optimized layouts for mobile (< 640px), tablet (640px - 1024px), and desktop (> 1024px).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shic-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/navigation.tsx
@src/components/hero-section.tsx
@src/components/experience-section.tsx
@src/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and optimize responsive breakpoints in Tailwind config</name>
  <files>src/tailwind.config.js</files>
<action>
Configure comprehensive breakpoints and ensure consistent responsive utilities.

Update src/tailwind.config.js:
```js
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      screens: {
        'xs': '475px',
        'sm': '640px',
        'md': '768px',
        'lg': '1024px',
        'xl': '1280px',
        '2xl': '1536px',
      },
      spacing: {
        '128': '32rem',
        '144': '36rem',
      },
      fontSize: {
        'xxs': '0.65rem',
      },
    },
  },
  plugins: [],
}
```

Breakpoints strategy:
- xs: 475px (small mobile)
- sm: 640px (large mobile)
- md: 768px (tablet)
- lg: 1024px (desktop)
- xl: 1280px (large desktop)
- 2xl: 1536px (extra large)
  </action>
  <verify>
- Tailwind config updated
- All breakpoints defined
- Spacing utilities added
- Font scale extended
  </verify>
  <done>
Responsive breakpoints configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Optimize navigation for mobile and tablet</name>
  <files>src/components/navigation.tsx</files>
<action>
Ensure navigation works perfectly across all devices with proper breakpoints and touch targets.

Update src/components/navigation.tsx:
```tsx
export function Navigation() {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const activeSection = useActiveSection(['hero', 'experience', 'skills', 'projects', 'about', 'contact']);

  return (
    <nav className="fixed top-0 left-0 right-0 z-50 bg-background/80 backdrop-blur-lg border-b">
      <div className="container mx-auto px-4">
        <div className="flex items-center justify-between h-16 md:h-20">
          {/* Logo */}
          <motion.a
            href="#hero"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
            className="text-lg md:text-xl font-bold"
          >
            RP
          </motion.a>

          {/* Desktop Navigation */}
          <div className="hidden md:flex items-center gap-1">
            {navItems.map((item) => (
              <Button
                key={item.id}
                variant="ghost"
                size="sm"
                className={`relative group transition-all duration-300 ${
                  activeSection === item.id
                    ? 'text-primary bg-primary/10'
                    : 'hover:text-primary hover:bg-primary/5'
                }`}
                onClick={() => scrollToSection(item.id)}
              >
                {item.label}
              </Button>
            ))}
          </div>

          {/* Theme Toggle & Mobile Menu Button */}
          <div className="flex items-center gap-2">
            <ThemeToggle />

            {/* Mobile Menu Button */}
            <Sheet open={mobileMenuOpen} onOpenChange={setMobileMenuOpen}>
              <SheetTrigger asChild>
                <Button
                  variant="ghost"
                  size="icon"
                  className="md:hidden"
                  aria-label="Open menu"
                >
                  <Menu className="h-6 w-6" />
                </Button>
              </SheetTrigger>

              <SheetContent side="right" className="w-[300px] sm:w-[400px]">
                <SheetHeader>
                  <SheetTitle>Navigation</SheetTitle>
                </SheetHeader>
                <div className="mt-8 flex flex-col gap-2">
                  {navItems.map((item) => (
                    <Button
                      key={item.id}
                      variant="ghost"
                      size="lg"
                      className={`justify-start transition-all duration-300 ${
                        activeSection === item.id
                          ? 'text-primary bg-primary/10'
                          : 'hover:text-primary hover:bg-primary/5'
                      }`}
                      onClick={() => {
                        scrollToSection(item.id);
                        setMobileMenuOpen(false);
                      }}
                    >
                      {item.label}
                    </Button>
                  ))}
                </div>
              </SheetContent>
            </Sheet>
          </div>
        </div>
      </div>
    </nav>
  );
}
```

Responsive optimizations:
- Hidden mobile menu on desktop (hidden md:hidden)
- Hidden desktop nav on mobile (hidden md:flex)
- Touch-friendly mobile sheet (300-400px width)
- Proper aria-labels for accessibility
- Safe area handling
- Adjusted heights (h-16 md:h-20)
- Proper spacing for touch targets (44px min)
  </action>
  <verify>
- Mobile menu opens correctly
- Desktop nav visible at md breakpoint
- Touch targets large enough (44px min)
- Sheet closes on link click
- Active state works in mobile
- Smooth transitions
- No horizontal overflow
  </verify>
  <done>
Navigation responsive optimized
  </done>
</task>

<task type="auto">
  <name>Task 3: Optimize hero and experience sections for all breakpoints</name>
  <files>src/components/hero-section.tsx, src/components/experience-section.tsx</files>
<action>
Ensure hero and experience sections use responsive typography, spacing, and layouts.

Update src/components/hero-section.tsx responsive typography:
```tsx
<motion.h1
  className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold mb-4 md:mb-6"
>
  Rohan Patel
</motion.h1>
<motion.p
  className="text-lg sm:text-xl md:text-2xl text-primary font-semibold mb-4"
>
  Senior Frontend Developer
</motion.p>
<motion.p
  className="text-sm sm:text-base md:text-lg text-muted-foreground max-w-2xl mx-auto mb-8 px-4"
>
  Building exceptional web experiences with modern technologies
</motion.p>

{/* CTA Buttons - Stack on mobile */}
<div className="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center px-4">
  <Button size="lg" className="w-full sm:w-auto">
    View Projects
  </Button>
  <Button size="lg" variant="outline" className="w-full sm:w-auto">
    Get In Touch
  </Button>
</div>
```

Update src/components/experience-section.tsx responsive timeline:
```tsx
{/* Responsive Timeline - Stack on mobile, alternate on desktop */}
<div className="relative">
  {/* Center line - hidden on mobile */}
  <div className="absolute left-4 sm:left-1/2 transform sm:-translate-x-1/2 h-full w-0.5 bg-primary/20" />

  <div className="space-y-8 md:space-y-12">
    {roles.map((role, index) => (
      <div
        key={role.id}
        className={`relative flex flex-col gap-4 md:gap-6 ${
          index % 2 === 0 ? 'md:flex-row' : 'md:flex-row-reverse'
        }`}
      >
        {/* Mobile dot vs Desktop dot */}
        <div className="absolute left-4 sm:left-1/2 transform -translate-x-1/2 w-4 h-4 bg-primary rounded-full border-4 border-background" />

        {/* Content - Full width on mobile */}
        <div className="ml-12 md:ml-0 flex-1">
          <Card className="p-4 md:p-6 lg:p-8">
            <h3 className="text-lg md:text-xl lg:text-2xl font-bold">
              {role.title}
            </h3>
            <p className="text-sm md:text-base text-primary">
              {role.company} • {role.startDate} - {role.endDate}
            </p>
          </Card>
        </div>
      </div>
    ))}
  </div>
</div>
```

Responsive optimizations:
- Typography scales at each breakpoint
- Buttons stack on mobile, row on desktop
- Padding adjustments (px-4 on mobile)
- Timeline stacks on mobile, alternates on desktop
- Card padding scales (p-4 md:p-6 lg:p-8)
- Text sizes responsive (text-sm md:text-base lg:text-lg)
- Margin adjustments for mobile (ml-12 for dot space)
  </action>
  <verify>
- Hero text readable on mobile
- CTA buttons stack correctly
- Timeline stacks on mobile
- Desktop alternation works
- No horizontal overflow
- Typography scales smoothly
- Touch targets adequate
- Spacing appropriate
  </verify>
  <done>
Hero and experience sections responsive optimized
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Tailwind breakpoints configured
- [ ] Navigation works on all devices
- [ ] Mobile menu functional
- [ ] Hero text scales properly
- [ ] Timeline stacks on mobile
- [ ] No horizontal overflow
- [ ] Touch targets adequate
- [ ] All breakpoints tested
</verification>

<success_criteria>
- All tasks completed
- Layout perfect at all breakpoints
- Typography scales smoothly
- Touch targets adequate (44px min)
- No horizontal scrolling
- Mobile experience polished
- Ready for performance optimization
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-performance/10-02-SUMMARY.md`:

# Phase 10 Plan 2: Optimize Responsive Design Summary

**Comprehensive responsive optimization across mobile, tablet, and desktop**

## Accomplishments
- Tailwind breakpoints configured (xs, sm, md, lg, xl, 2xl)
- Navigation mobile menu (Sheet) functional
- Desktop nav hidden on mobile, vice versa
- Hero typography scales (text-4xl to text-7xl)
- CTA buttons stack on mobile
- Timeline stacks on mobile, alternates on desktop
- Card padding scales (p-4 to p-8)
- Touch targets optimized (44px minimum)
- Mobile margins adjusted (ml-12 for timeline)
- No horizontal overflow
- Smooth breakpoint transitions

## Files Created/Modified
- `src/tailwind.config.js` - Extended breakpoints
- `src/components/navigation.tsx` - Mobile menu
- `src/components/hero-section.tsx` - Responsive typography
- `src/components/experience-section.tsx` - Responsive timeline

## Decisions Made
- 6 breakpoints (comprehensive coverage)
- Mobile-first approach (base styles mobile)
- Stack on mobile (better UX)
- Touch targets 44px (accessibility)
- Sheet width 300-400px (mobile menu)
- Typography scales at each breakpoint
- Timeline alternates on desktop only (visual interest)

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
Ready for 10-03-PLAN.md (Performance optimization)
</output>
