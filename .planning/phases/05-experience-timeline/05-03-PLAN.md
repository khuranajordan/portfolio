---
phase: 05-experience-timeline
plan: 03
type: execute
domain: ui-design
---

<objective>
Add detailed role information with expandable content and achievement highlights.

Purpose: Expand timeline entries with full responsibilities, achievements, and interactive details for comprehensive experience presentation.
Output: Enhanced timeline with expandable role cards showing complete information and key achievements.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@.planning/phases/02-theme-system/02-01-SUMMARY.md
@.planning/phases/02-theme-system/02-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-01-SUMMARY.md
@.planning/phases/03-layout-structure/03-02-SUMMARY.md
@.planning/phases/03-layout-structure/03-03-SUMMARY.md
@.planning/phases/04-hero-section/04-01-SUMMARY.md
@.planning/phases/04-hero-section/04-02-SUMMARY.md
@.planning/phases/04-hero-section/04-03-SUMMARY.md
@.planning/phases/05-experience-timeline/05-01-SUMMARY.md
@.planning/phases/05-experience-timeline/05-02-SUMMARY.md
@src/components/experience-timeline.tsx
@src/types/experience.ts
@src/components/ui/collapsible.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add achievements and expandable details</name>
  <files>src/types/experience.ts, src/components/experience-timeline.tsx</files>
  <action>
Add achievements to experience data and create expandable cards using shadcn Collapsible component.

Update src/types/experience.ts to add achievements field:
Add to each experience object:
```tsx
achievements?: string[];
```

Add achievements to relevant roles:
```tsx
achievements: [
  'Promoted to Senior role within 1 year',
  'Led development of key frontend features',
  'Mentored 3 junior developers'
]
```

Install Collapsible component:
Run: npx shadcn@latest add collapsible

Update src/components/experience-timeline.tsx:
Add Collapsible imports and update role card:
```tsx
import {
  Collapsible,
  CollapsibleContent,
  CollapsibleTrigger,
} from '@/components/ui/collapsible';
import { ChevronDown } from 'lucide-react';
import { useState } from 'react';

function TimelineEntry({ children, index, isAlternate }: TimelineEntryProps) {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <motion.div
      // ... existing motion props ...
    >
      {/* ... dot indicator and date badge ... */}

      {/* Role Card with Collapsible */}
      <Collapsible open={isOpen} onOpenChange={setIsOpen}>
        <CollapsibleTrigger asChild>
          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true, margin: '-100px' }}
            transition={{
              duration: 0.5,
              delay: index * 0.1 + 0.2
            }}
            className="bg-card border rounded-lg p-6 shadow-sm hover:shadow-md transition-all cursor-pointer group"
          >
            <div className="flex items-start justify-between gap-4">
              <div className="flex-1">
                <h3 className="text-xl md:text-2xl font-bold mb-2">
                  {exp.title}
                </h3>
                <p className="text-primary font-semibold text-base mb-2">
                  {exp.company}
                </p>
                <div className="flex items-center gap-2 text-sm text-muted-foreground mb-4">
                  <MapPin className="w-3 h-3" />
                  <span>{exp.location}</span>
                </div>
                <p className="text-sm text-foreground mb-4 leading-relaxed">
                  {exp.description}
                </p>
              </div>
              <ChevronDown
                className={`w-5 h-5 text-muted-foreground transition-transform duration-300 flex-shrink-0 ${
                  isOpen ? 'rotate-180' : ''
                }`}
              />
            </div>
          </motion.div>
        </CollapsibleTrigger>

        <CollapsibleContent className="mt-4 pt-4 border-t space-y-4">
          {/* Full Responsibilities */}
          <div>
            <p className="text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-2">
              Responsibilities
            </p>
            <ul className="text-sm text-foreground space-y-2">
              {exp.responsibilities.map((resp, i) => (
                <li key={i} className="flex items-start gap-2">
                  <span className="text-primary mt-0.5 flex-shrink-0">•</span>
                  <span>{resp}</span>
                </li>
              ))}
            </ul>
          </div>

          {/* Achievements */}
          {exp.achievements && exp.achievements.length > 0 && (
            <div>
              <p className="text-xs font-semibold text-muted-foreground uppercase tracking-wider mb-2">
                Key Achievements
              </p>
              <ul className="text-sm text-foreground space-y-2">
                {exp.achievements.map((achievement, i) => (
                  <li key={i} className="flex items-start gap-2">
                    <span className="text-green-500 mt-0.5 flex-shrink-0">✓</span>
                    <span>{achievement}</span>
                  </li>
                ))}
              </ul>
            </div>
          )}

          {/* Tech Stack Badge */}
          <div className="flex flex-wrap gap-2">
            <Badge variant="secondary">React</Badge>
            <Badge variant="secondary">TypeScript</Badge>
            <Badge variant="secondary">Tailwind CSS</Badge>
          </div>
        </CollapsibleContent>
      </Collapsible>
    </motion.div>
  );
}
```

Enhancements:
- Collapsible cards (click to expand)
- Chevron icon rotates on open/close
- Full responsibilities list
- Achievements section with checkmarks
- Tech stack badges
- Cursor pointer on entire card
- Hover shadow effect
  </action>
  <verify>
- Collapsible component installed
- Cards expand/collapse on click
- Chevron icon rotates correctly
- All responsibilities visible when expanded
- Achievements display with checkmarks
- Tech stack badges visible
- Smooth expand/collapse animation
  </verify>
  <done>
Expandable role details implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Add visual enhancements and interactions</name>
  <files>src/components/experience-timeline.tsx</files>
  <action>
Add polished visual effects including hover states, better spacing, and mobile improvements.

Update src/components/experience-timeline.tsx with final polish:

1. Enhance card trigger area and visual feedback:
```tsx
<CollapsibleTrigger asChild>
  <motion.div
    // ... existing motion props ...
    className="bg-card border rounded-lg p-6 shadow-sm hover:shadow-md hover:border-primary/50 transition-all cursor-pointer group"
  >
    {/* ... content ... */}
  </motion.div>
</CollapsibleTrigger>
```

2. Add "View Details" / "Hide Details" text:
Update chevron section:
```tsx
<div className="flex items-center justify-between w-full">
  <div className="flex-1">
    {/* ... title, company, location, description ... */}
  </div>
  <div className="flex items-center gap-2 flex-shrink-0">
    <span className="text-xs text-muted-foreground group-hover:text-primary transition-colors hidden sm:inline-block">
      {isOpen ? 'Hide' : 'View'} Details
    </span>
    <ChevronDown
      className={`w-5 h-5 text-muted-foreground transition-transform duration-300 ${
        isOpen ? 'rotate-180' : ''
      }`}
    />
  </div>
</div>
```

3. Improve mobile spacing and responsiveness:
Add mobile-specific classes:
- Reduce padding on mobile: `p-4 md:p-6`
- Adjust text sizes: `text-base md:text-xl` for title
- Improve touch targets on mobile

4. Add subtle pulse animation to first dot (current role):
```tsx
<motion.div
  initial={{ scale: 0 }}
  whileInView={{ scale: 1 }}
  viewport={{ once: true, margin: '-50px' }}
  animate={isFirst ? {
    scale: [1, 1.1, 1],
    transition: {
      duration: 2,
      repeat: Infinity,
      repeatDelay: 1
    }
  } : {}}
  // ... rest of props ...
/>
```

Visual enhancements:
- Border color change on hover (primary/50)
- "View/Hide Details" text (desktop only)
- Pulse animation on first dot (current role)
- Better mobile spacing
- Improved touch targets
- Smoother transitions
  </action>
  <verify>
- Hover states working (border color)
- "View Details" text appears/disappears
- Pulse animation on first dot
- Mobile spacing improved
- All interactions smooth
- Works in both themes
  </verify>
  <done>
Timeline fully polished with visual enhancements
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Collapsible cards functional
- [ ] Full responsibilities shown
- [ ] Achievements displayed
- [ ] Visual enhancements working
- [ ] Mobile responsiveness verified
- [ ] Interactions smooth
</verification>

<success_criteria>
- All tasks completed
- Timeline fully detailed and expandable
- Complete experience information
- Professional visual polish
- **Phase 5 complete** - ready for Phase 6 (Skills Visualization)
</success_criteria>

<output>
After completion, create `.planning/phases/05-experience-timeline/05-03-SUMMARY.md`:

# Phase 5 Plan 3: Add Role Details Summary

**Timeline enhanced with expandable cards, full responsibilities, achievements, and polished visual effects**

## Accomplishments
- Collapsible card functionality
- Full responsibilities list (not just preview)
- Achievements section with checkmarks
- Tech stack badges
- "View/Hide Details" indicator
- Hover border effect (primary/50)
- Pulse animation on current role dot
- Improved mobile responsiveness
- Complete interaction feedback

## Files Created/Modified
- `src/types/experience.ts` - Added achievements field
- `src/components/experience-timeline.tsx` - All enhancements
- `src/components/ui/collapsible.tsx` - Installed component

## Decisions Made
- Click anywhere on card to expand (better UX)
- Green checkmarks for achievements (visual positivity)
- Badge tech stack (contextual info)
- "View/Hide Details" text desktop only (mobile space)
- Pulse on first dot (draw attention to current role)
- Border hover effect (interactive feedback)
- Full details on demand (avoid clutter)

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
**Phase 5 complete** - ready for Phase 6 (Skills Visualization)
</output>
