---
phase: 01-foundation-setup
plan: 03
type: execute
domain: ui-design
---

<objective>
Set up shadcn/ui component library and create base component structure for the portfolio.

Purpose: Establish a component library foundation with pre-built, accessible components that follow modern design patterns.
Output: shadcn/ui configured with components.json, base utilities installed, sample components created.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
@package.json
@tailwind.config.js
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure shadcn/ui</name>
  <files>package.json, components.json, tsconfig.json, tailwind.config.js, src/lib/utils.ts</files>
  <action>
Initialize shadcn/ui with proper configuration for the project.

Steps:
1. Run: npm install -D @types/node
2. Run: npx shadcn@latest init

When prompted during shadcn init:
- TypeScript: Yes
- Style: Default (New York)
- Base color: Slate
- CSS variables: Yes
- React Server Components: No (this is client-side Vite)
- Path alias: @/* (already configured)

This creates:
- components.json (shadcn configuration)
- src/lib/utils.ts (cn utility function)
- Updated tailwind.config.js with shadcn CSS variables
- Updated src/index.css with shadcn base CSS

Verify:
- components.json exists with correct paths
- src/lib/utils.ts has cn() function for class merging
- tailwind.config.js has shadcn theme extension
- src/index.css has @layer base with CSS variables
  </action>
  <verify>
- components.json exists in project root
- src/lib/utils.ts exists with cn function
- package.json has @types/node in devDependencies
- npm run build completes successfully
- No TypeScript errors
  </verify>
  <done>
shadcn/ui initialized with proper configuration and utilities
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sample components and create base structure</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/App.tsx</files>
  <action>
Add a few essential shadcn/ui components and test them with a sample layout.

Install components:
1. Run: npx shadcn@latest add button
2. Run: npx shadcn@latest add card
3. Run: npx shadcn@latest add badge

This creates components in src/components/ui/.

Update src/App.tsx with a test layout:
```tsx
import { Button } from '@/components/ui/button';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';

function App() {
  return (
    <div className="min-h-screen bg-background text-foreground p-8">
      <div className="max-w-4xl mx-auto">
        <Card>
          <CardHeader>
            <CardTitle className="text-3xl font-bold">Portfolio Foundation</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <p className="text-muted-foreground">
              shadcn/ui components configured and ready
            </p>
            <div className="flex gap-2">
              <Badge variant="default">React</Badge>
              <Badge variant="secondary">TypeScript</Badge>
              <Badge variant="outline">Tailwind</Badge>
            </div>
            <div className="flex gap-2 pt-4">
              <Button>Primary Action</Button>
              <Button variant="secondary">Secondary</Button>
              <Button variant="outline">Outline</Button>
            </div>
          </CardContent>
        </Card>
      </div>
    </div>
  );
}

export default App;
```

Create src/components/ directory structure if it doesn't exist.

Verify components render correctly with dark/light mode (toggle will be added in Phase 2).
  </action>
  <verify>
- src/components/ui/button.tsx exists
- src/components/ui/card.tsx exists
- src/components/ui/badge.tsx exists
- src/App.tsx uses shadcn components
- npm run dev shows styled components rendering
- Components respond to theme changes (test by manually adding 'dark' class to HTML)
  </verify>
  <done>
shadcn/ui components installed and working, base component structure created
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
shadcn/ui component library configured with Button, Card, and Badge components. Test layout created in App.tsx showing:
- Card with title and content
- Badge components (React, TypeScript, Tailwind labels)
- Three button variants (Primary, Secondary, Outline)
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173
    3. Verify:
       - Card renders with proper padding and shadow
       - Title is bold and sized correctly
       - Badges display with different styles (default, secondary, outline)
       - Three buttons show distinct visual styles
       - All components use Tailwind utilities
       - No console errors
    4. Test dark mode (optional preview):
       - Open browser DevTools
       - Add class="dark" to <html> element
       - Verify colors adapt to dark theme
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] shadcn/ui fully configured (components.json, utils.ts)
- [ ] Sample components added and working
- [ ] App.tsx renders test layout successfully
- [ ] Dark mode CSS variables configured (ready for Phase 2)
- [ ] npm run build completes without errors
</verification>

<success_criteria>
- All tasks completed
- shadcn/ui component library integrated
- Base component structure established
- Components render correctly in both light and dark themes
- Foundation complete for feature development
- **Phase 1 complete** - ready for Phase 2 (Theme System)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Set Up shadcn/ui Summary

**shadcn/ui component library configured with Button, Card, Badge components and base project structure**

## Accomplishments
- shadcn/ui initialized with proper configuration
- Utility functions (cn) created
- Essential UI components installed (Button, Card, Badge)
- Base component structure established (src/components/ui/)
- Test layout demonstrating components
- Dark mode CSS variables configured

## Files Created/Modified
- `components.json` - shadcn/ui configuration
- `src/lib/utils.ts` - cn utility for class merging
- `src/components/ui/button.tsx` - Button component
- `src/components/ui/card.tsx` - Card components
- `src/components/ui/badge.tsx` - Badge component
- `src/App.tsx` - Test layout with shadcn components
- `src/index.css` - Updated with shadcn CSS variables
- `tailwind.config.js` - Extended with shadcn theme
- `package.json` - Added @types/node

## Decisions Made
- Used default shadcn style (New York) for modern, clean aesthetic
- Slate as base color (matches portfolio theme)
- Client-side only (no RSC - this is Vite, not Next.js)
- @types/node for utility functions

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
**Phase 1 complete** - ready for Phase 2 (Theme System)
</output>
