---
phase: 01-foundation-setup
plan: 02
type: execute
domain: ui-design
---

<objective>
Configure Tailwind CSS with custom theme and optimization for the portfolio.

Purpose: Establish the styling foundation with utility-first CSS framework and custom design tokens.
Output: Tailwind CSS fully configured with custom theme, proper file structure, and optimized build settings.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
@package.json
@vite.config.ts
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Tailwind CSS</name>
  <files>package.json, tailwind.config.js, postcss.config.js, src/index.css, src/App.tsx</files>
  <action>
Install Tailwind CSS and its dependencies, then configure for Vite project.

Steps:
1. Run: npm install -D tailwindcss postcss autoprefixer
2. Run: npx tailwindcss init -p (creates tailwind.config.js and postcss.config.js)

Update tailwind.config.js:
- Set content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
- Configure theme section for future customization (keep defaults for now)
- Add darkMode: 'class' to prepare for theme system in Phase 2

Update postcss.config.js:
- Verify plugins: tailwindcss, autoprefixer are configured

Create src/index.css (or overwrite existing):
- Add Tailwind directives:
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

Update src/App.tsx:
- Remove any existing imports or CSS references
- Add simple component with Tailwind classes:
  function App() {
    return <div className="p-4 text-lg font-bold">Portfolio Setup</div>;
  }

Verify Tailwind is working by running dev server and checking styles.
  </action>
  <verify>
- package.json contains tailwindcss, postcss, autoprefixer in devDependencies
- tailwind.config.js exists with correct content paths
- postcss.config.js exists with plugins configured
- src/index.css contains @tailwind directives
- npm run dev shows Tailwind styles applied (text is bold, padded)
- Build output includes Tailwind CSS
  </verify>
  <done>
Tailwind CSS installed, configured, and applying utility classes successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Create custom theme configuration</name>
  <files>tailwind.config.js</files>
  <action>
Extend Tailwind theme with custom design tokens for the portfolio.

Update tailwind.config.js theme section:

module.exports = {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        // Primary colors for CTAs and highlights
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
        },
        // Neutral/slate for text and backgrounds
        neutral: {
          50: '#f8fafc',
          100: '#f1f5f9',
          200: '#e2e8f0',
          300: '#cbd5e1',
          400: '#94a3b8',
          500: '#64748b',
          600: '#475569',
          700: '#334155',
          800: '#1e293b',
          900: '#0f172a',
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['Fira Code', 'Monaco', 'monospace'],
      },
      fontSize: {
        'hero': ['3.5rem', { lineHeight: '1.1', letterSpacing: '-0.02em' }],
        'section': ['2.5rem', { lineHeight: '1.2', letterSpacing: '-0.01em' }],
      },
      spacing: {
        '128': '32rem',
        '144': '36rem',
      }
    },
  },
  plugins: [],
};

This provides:
- Primary blue color palette for CTAs
- Neutral slate palette for professional appearance
- Font families optimized for portfolio (will add Inter in future)
- Hero and section heading sizes
- Extended spacing for layout
  </action>
  <verify>
- tailwind.config.js has theme.extend with custom colors, fonts, spacing
- No TypeScript or build errors
- Theme values are accessible as utility classes (e.g., text-primary-500, bg-neutral-900)
  </verify>
  <done>
Custom theme configured with professional color palette and design tokens
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Tailwind CSS directives in src/index.css
- [ ] Custom theme tokens defined in tailwind.config.js
- [ ] Dark mode configured for class-based toggling
- [ ] npm run build completes with Tailwind processing
- [ ] Dev server shows Tailwind utility classes working
</verification>

<success_criteria>
- All tasks completed
- Tailwind CSS fully integrated
- Custom theme configured
- Build succeeds with Tailwind optimizations
- Ready for shadcn/ui installation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Configure Tailwind CSS Summary

**Tailwind CSS configured with custom theme, dark mode support, and professional design tokens**

## Accomplishments
- Tailwind CSS installed and integrated with Vite
- Custom color palette (primary blue, neutral slate)
- Font families configured (Inter, Fira Code)
- Dark mode foundation (class-based)
- Extended spacing and typography scale

## Files Created/Modified
- `package.json` - Added tailwindcss, postcss, autoprefixer
- `tailwind.config.js` - Custom theme configuration
- `postcss.config.js` - PostCSS plugins
- `src/index.css` - Tailwind directives
- `src/App.tsx` - Test component with Tailwind

## Decisions Made
- Class-based dark mode for Phase 2 theme system
- Slate neutral palette for professional appearance
- Primary blue for CTAs and interactive elements
- Inter for sans-serif (modern, clean), Fira Code for code display

## Deviations from Plan
None - plan executed as specified.

## Issues Encountered
None

## Next Step
Ready for 01-03-PLAN.md (Set up shadcn/ui)
</output>
