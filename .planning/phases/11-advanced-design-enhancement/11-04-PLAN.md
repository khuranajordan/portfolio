---
phase: 11-advanced-design-enhancement
plan: 04
type: execute
domain: ui-design
---

<objective>
Implement sophisticated glassmorphism and depth effects throughout the portfolio.

Add frosted glass effects, layered shadows, subtle borders, and ambient glows to create premium depth and visual hierarchy across all components.

Purpose: Apply Apple's signature glassmorphism and depth techniques to elevate the visual sophistication and create a premium, layered aesthetic.

Output: Enhanced cards, navigation, and sections with glassmorphism, depth shadows, and ambient glow effects.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-advanced-design-enhancement/DESIGN-SYSTEM.md
@.planning/phases/11-advanced-design-enhancement/11-01-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-02-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-03-SUMMARY.md
@src/components/navigation.tsx
@src/components/experience-timeline.tsx
@src/components/project-card.tsx
@src/components/case-study.tsx
@src/index.css

**Tech stack available:**
- Glassmorphism utility from 11-01
- Tailwind CSS v4 backdrop utilities
- Framer Motion for hover effects
- Dark mode support

**Established patterns:**
- Basic glass-card utility exists
- Bento grids in place from 11-03
- Mesh gradient backgrounds working

**Current depth effects:**
- Basic shadows (hover:shadow-lg)
- Simple borders (border)
- Backdrop blur on navigation

**Apple glassmorphism patterns:**
- Multi-layered shadows (subtle → pronounced)
- Frosted glass with backdrop-blur-xl (20px+)
- Ultra-subtle borders (1px, 5-10% opacity)
- Ambient glow effects on hover
- Depth through layered elements
- Smooth transitions (300-500ms)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance glassmorphism system with sophisticated depth effects</name>
  <files>src/index.css</files>
  <action>Create advanced glassmorphism utility classes:

1. **Add premium glass card variants** to src/index.css:
```css
@layer utilities {
  /* Premium glass with depth */
  .glass-premium {
    background: oklch(1 0 0 / 0.05);
    backdrop-filter: blur(24px) saturate(180%);
    border: 1px solid oklch(1 0 0 / 0.08);
    box-shadow:
      0 1px 2px 0 oklch(0 0 0 / 0.05),
      0 4px 12px 0 oklch(0 0 0 / 0.08),
      inset 0 0 0 1px oklch(1 0 0 / 0.05);
  }

  .dark .glass-premium {
    background: oklch(1 0 0 / 0.03);
    border: 1px solid oklch(1 0 0 / 0.06);
    box-shadow:
      0 1px 2px 0 oklch(0 0 0 / 0.3),
      0 4px 12px 0 oklch(0 0 0 / 0.4),
      inset 0 0 0 1px oklch(1 0 0 / 0.03);
  }

  /* Subtle glass for less prominent elements */
  .glass-subtle {
    background: oklch(1 0 0 / 0.03);
    backdrop-filter: blur(16px);
    border: 1px solid oklch(1 0 0 / 0.05);
    box-shadow: 0 2px 8px 0 oklch(0 0 0 / 0.06);
  }

  .dark .glass-subtle {
    background: oklch(1 0 0 / 0.02);
    border: 1px solid oklch(1 0 0 / 0.04);
  }

  /* Elevated card with glow */
  .glass-elevated {
    background: oklch(1 0 0 / 0.06);
    backdrop-filter: blur(20px) saturate(160%);
    border: 1px solid oklch(1 0 0 / 0.1);
    box-shadow:
      0 4px 8px 0 oklch(0 0 0 / 0.08),
      0 12px 32px 0 oklch(0 0 0 / 0.12),
      inset 0 0 0 1px oklch(1 0 0 / 0.06);
  }

  .dark .glass-elevated {
    background: oklch(1 0 0 / 0.04);
    box-shadow:
      0 4px 8px 0 oklch(0 0 0 / 0.4),
      0 12px 32px 0 oklch(0 0 0 / 0.5),
      inset 0 0 0 1px oklch(1 0 0 / 0.04);
  }

  /* Ambient glow on hover */
  .glass-glow {
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .glass-glow:hover {
    border-color: oklch(0.55 0.12 250 / 0.3);
    box-shadow:
      0 8px 16px 0 oklch(0 0 0 / 0.1),
      0 24px 48px 0 oklch(0 0 0 / 0.15),
      0 0 0 1px oklch(0.55 0.12 250 / 0.1),
      inset 0 0 0 1px oklch(1 0 0 / 0.08);
  }

  .dark .glass-glow:hover {
    border-color: oklch(0.55 0.12 250 / 0.4);
    box-shadow:
      0 8px 16px 0 oklch(0 0 0 / 0.5),
      0 24px 48px 0 oklch(0 0 0 / 0.6),
      0 0 0 1px oklch(0.55 0.12 250 / 0.2),
      inset 0 0 0 1px oklch(1 0 0 / 0.06);
  }
}
```

2. **Add shadow scale utilities**:
```css
@layer utilities {
  .shadow-ambient {
    box-shadow: 0 2px 16px 0 oklch(0 0 0 / 0.08);
  }
  .dark .shadow-ambient {
    box-shadow: 0 2px 16px 0 oklch(0 0 0 / 0.4);
  }

  .shadow-floating {
    box-shadow:
      0 4px 12px 0 oklch(0 0 0 / 0.1),
      0 0 0 1px oklch(1 0 0 / 0.05);
  }
  .dark .shadow-floating {
    box-shadow:
      0 4px 12px 0 oklch(0 0 0 / 0.5),
      0 0 0 1px oklch(1 0 0 / 0.03);
  }
}
```

3. **Update existing glass-card** to use premium variant:
- Keep original glass-card for backward compatibility
- Add glass-premium as the new default

DO NOT:
- Don't use heavy backdrop-blur (>40px) - kills performance
- Don't use opaque backgrounds (must be <10% opacity)
- Don't use harsh shadows (must be subtle and layered)
</action>
  <verify>CSS validates, new utilities render correctly, glass effects work in light and dark modes, transitions are smooth</verify>
  <done>Advanced glassmorphism utilities created with multi-layered shadows, ambient glow on hover, and premium depth effects</done>
</task>

<task type="auto">
  <name>Task 2: Apply glassmorphism to navigation and timeline components</name>
  <files>src/components/navigation.tsx, src/components/experience-timeline.tsx</files>
  <action>Update navigation and timeline with premium glassmorphism:

1. **Enhance navigation** in src/components/navigation.tsx:
```tsx
// Find the sticky nav container
<header className="sticky top-0 z-50 w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
  {/* Update to */}
  <header className="sticky top-0 z-50 w-full border-b border-white/10 glass-premium">
```

2. **Add depth to mobile menu** (sheet/dialog):
```tsx
// Find the Sheet component and add glass effect
<Sheet>
  <SheetContent className="glass-premium border-r border-white/10 w-full sm:w-80">
    {/* ... menu items */}
  </SheetContent>
</Sheet>
```

3. **Enhance timeline cards** in src/components/experience-timeline.tsx:
```tsx
// Find timeline entry cards and update
<motion.div
  className={cn(
    "relative p-6 md:p-8 rounded-2xl glass-premium hover:glass-elevated transition-all duration-500",
    "border-l-4 border-primary/50 hover:border-primary"
  )}
  whileHover={{
    y: -4,
    transition: { duration: 0.3 }
  }}
>
  {/* ... card content */}
</motion.div>
```

4. **Add glow to timeline dots**:
```tsx
// Find the timeline dot elements
<motion.div
  className="w-4 h-4 rounded-full bg-primary shadow-[0_0_20px_rgba(59,130,246,0.5)] dark:shadow-[0_0_30px_rgba(59,130,246,0.3)]"
  animate={{
    scale: [1, 1.1, 1],
    opacity: [0.8, 1, 0.8]
  }}
  transition={{
    duration: 2,
    repeat: Infinity,
    ease: "easeInOut"
  }}
/>
```

5. **Add depth to collapsible content**:
```tsx
// Find Collapsible content and add glass effect
<CollapsibleContent className="glass-subtle mt-4 rounded-xl p-4 border-t">
  {/* ... details */}
</CollapsibleContent>
```

DO NOT:
- Don't break navigation functionality
- Don't overdo the glow effects (subtle is key)
- Don't compromise text readability
</action>
  <verify>Navigation has glass effect, timeline cards have depth, mobile menu has glassmorphism, all hover states work smoothly</verify>
  <done>Navigation and timeline enhanced with premium glassmorphism, elevated shadows on hover, subtle glow on timeline dots</done>
</task>

<task type="auto">
  <name>Task 3: Apply glassmorphism and depth to project components</name>
  <files>src/components/project-card.tsx, src/components/case-study.tsx</files>
  <action>Enhance project cards with sophisticated depth effects:

1. **Update project cards** in src/components/project-card.tsx:
```tsx
// Find the card container
<motion.div
  className={cn(
    "glass-premium rounded-3xl overflow-hidden glass-glow",
    "hover:glass-elevated",
    className
  )}
  whileHover={{
    y: -8,
    scale: 1.02,
    transition: { duration: 0.4, ease: [0.4, 0, 0.2, 1] }
  }}
>
  {/* ... card content */}
</motion.div>
```

2. **Add depth to tech stack badges**:
```tsx
// Find badge rendering and enhance
<span className="glass-subtle px-3 py-1 rounded-full text-sm font-medium border border-white/10 hover:border-primary/30 transition-colors">
  {tech}
</span>
```

3. **Enhance case study components** in src/components/case-study.tsx:
```tsx
// Add depth to feature/achievement lists
<div className="glass-subtle rounded-2xl p-6 space-y-4">
  <div className="flex items-start gap-3">
    <div className="w-6 h-6 rounded-lg bg-primary/10 flex items-center justify-center flex-shrink-0 mt-0.5">
      <Check className="w-4 h-4 text-primary" />
    </div>
    <p className="text-sm">{feature}</p>
  </div>
</div>
```

4. **Add ambient glow** to featured project cards:
```tsx
// For showcase/featured projects, add glow effect
<motion.div
  className="glass-elevated rounded-3xl overflow-hidden"
  animate={{
    boxShadow: [
      "0 4px 12px 0 oklch(0 0 0 / 0.1)",
      "0 8px 24px 0 oklch(0 0 0 / 0.15)",
      "0 4px 12px 0 oklch(0 0 0 / 0.1)"
    ]
  }}
  transition={{
    duration: 4,
    repeat: Infinity,
    ease: "easeInOut"
  }}
>
  {/* ... featured project content */}
</motion.div>
```

5. **Add depth to external links**:
```tsx
// Find GitHub/Live demo links and add depth
<a
  href={githubUrl}
  className="glass-subtle px-4 py-2 rounded-xl flex items-center gap-2 hover:glass-elevated transition-all duration-300 border border-white/10 hover:border-primary/30"
>
  <Github className="w-4 h-4" />
  <span>View Code</span>
</a>
```

DO NOT:
- Don't break card functionality or links
- Don't add too much glow (keep it subtle)
- Don't make shadows too harsh
</action>
  <verify>Project cards have depth, hover effects work smoothly, tech badges have glass effect, links have elevated states</verify>
  <done>Project components enhanced with premium glassmorphism, ambient glow animations, depth shadows, and elevated hover states</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All glassmorphism utilities work in light and dark modes
- [ ] Navigation has premium glass effect
- [ ] Timeline cards have depth with elevated hover
- [ ] Project cards have multi-layered shadows and glow
- [ ] Tech badges have subtle glass effect
- [ ] All animations are smooth (60fps)
- [ ] Text remains readable on all glass surfaces
</verification>

<success_criteria>

- Advanced glassmorphism utilities created
- Navigation, timeline, and projects enhanced
- Multi-layered shadows for depth
- Ambient glow on hover for key elements
- Smooth transitions (300-500ms)
- Works in both light and dark modes
- Text readability maintained
</success_criteria>

<output>
After completion, create `.planning/phases/11-advanced-design-enhancement/11-04-SUMMARY.md`:

# Phase 11 Plan 4: Glassmorphism & Depth Summary

**Implemented sophisticated glassmorphism and depth effects with multi-layered shadows, ambient glows, and premium frosted glass aesthetics.**

## Accomplishments

- Created advanced glassmorphism utility system (premium, subtle, elevated)
- Enhanced navigation with premium glass effect
- Applied depth effects to timeline and project cards
- Added ambient glow animations on hover
- Multi-layered shadows for sophisticated depth

## Files Created/Modified

- `src/index.css` - Advanced glassmorphism utilities and shadow scales
- `src/components/navigation.tsx` - Premium glass effect
- `src/components/experience-timeline.tsx` - Depth and glow effects
- `src/components/project-card.tsx` - Glassmorphism and elevated hover
- `src/components/case-study.tsx` - Subtle glass effects

## Decisions Made

- Backdrop blur of 24px for premium glass (optimal balance)
- Multi-layered shadows (3 layers) for sophisticated depth
- Ambient glow with 400ms cubic-bezier transitions
- Subtle borders at 5-10% opacity
- Glass elevate on hover with -8px y translation

## Next Step

Ready for 11-05-PLAN.md - Typography refinement and final polish
</output>
