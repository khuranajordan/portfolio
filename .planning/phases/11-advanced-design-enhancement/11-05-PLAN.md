---
phase: 11-advanced-design-enhancement
plan: 05
type: execute
domain: ui-design
---

<objective>
Refine typography system and apply final visual polish across all sections.

Optimize typography scales, spacing, and visual consistency to achieve Apple-level polish with perfect attention to detail.

Purpose: Complete the Apple-inspired transformation with refined typography, optimized spacing, and meticulous visual polish throughout the portfolio.

Output: Polished portfolio with Apple-level typography sophistication, consistent spacing, and premium visual refinement.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-advanced-design-enhancement/DESIGN-SYSTEM.md
@.planning/phases/11-advanced-design-enhancement/11-01-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-02-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-03-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-04-SUMMARY.md
@src/components/hero.tsx
@src/components/about-section.tsx
@src/components/contact-section.tsx
@src/components/footer.tsx
@src/index.css

**Tech stack available:**
- Typography system from 11-01
- All glassmorphism and depth effects
- Bento grid layouts
- Mesh gradient backgrounds

**Established patterns:**
- Display and hero typography scales exist
- Glassmorphism applied throughout
- Bento grids in place

**Current state:**
- Typography utilities created but not fully applied
- Some sections may need spacing refinement
- Final polish touches needed for consistency

**Apple typography polish:**
- Tight line heights on headings (1.1-1.15)
- Negative letter-spacing for large headings
- Generous paragraph spacing (1.6-1.7 line-height)
- Consistent spacing scale throughout
- Strategic use of font weights
- Perfect alignment and grid adherence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Apple-inspired typography refinements to hero and headings</name>
  <files>src/components/hero.tsx, src/index.css</files>
  <action>Refine typography throughout the portfolio with Apple-level polish:

1. **Update hero typography** in src/components/hero.tsx:
```tsx
// Name with display scale
<h1 className="text-display font-bold mb-8 md:mb-12 tracking-tight">
  <motion.span
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ duration: 0.8 }}
    className="text-primary inline-block"
  >
    Rohan
  </motion.span>{" "}
  <motion.span
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    transition={{ duration: 0.8, delay: 0.3 }}
    className="inline-block"
  >
    Khurana
  </motion.span>
</h1>

// Title with hero scale
<h2 className="text-hero font-semibold text-primary mb-8 tracking-tight">
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5 }}
    className="inline-block"
  >
    Senior
  </motion.span>{" "}
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5, delay: 0.1 }}
    className="inline-block"
  >
    Frontend
  </motion.span>{" "}
  <motion.span
    initial={{ opacity: 0, y: 10 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.5, delay: 0.2 }}
    className="inline-block"
  >
    Developer
  </motion.span>
</h2>

// Value proposition with refined body typography
<motion.p
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6, delay: 0.2 }}
  className="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto leading-relaxed px-4 mb-12 tracking-wide"
>
  I build exceptional digital experiences with React, TypeScript, and
  modern web technologies. Transforming ideas into polished,
  performant applications.
</motion.p>
```

2. **Add global typography utilities** to src/index.css:
```css
@layer base {
  /* Refined body text */
  body {
    @apply bg-background text-foreground;
    font-feature-settings: "cv11", "ss01";
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Optimized heading styles */
  h1, h2, h3, h4, h5, h6 {
    @apply tracking-tight;
    font-feature-settings: "cv11", "ss01";
  }

  h1 {
    @apply text-display font-bold;
  }

  h2 {
    @apply text-hero font-semibold;
  }

  h3 {
    @apply text-section font-semibold;
  }

  /* Refined paragraph text */
  p {
    @apply leading-relaxed;
    letter-spacing: -0.003em;
  }

  /* Smooth text selection */
  ::selection {
    background-color: oklch(0.55 0.12 250 / 0.3);
    color: inherit;
  }
}
```

3. **Add section heading utility**:
```css
@layer utilities {
  .section-heading {
    @apply text-section font-semibold tracking-tight mb-6;
  }

  .sub-section-heading {
    @apply text-2xl md:text-3xl font-semibold tracking-tight mb-4;
  }
}
```

DO NOT:
- Don't make headings too tight (unreadable)
- Don't overdo negative letter-spacing
- Don't break responsive typography
</action>
  <verify>Hero text uses display/hero scales, headings have tracking-tight, body text has leading-relaxed, typography is responsive</verify>
  <done>Typography refined with display/hero scales, tight tracking, optimized line heights, and responsive clamping</done>
</task>

<task type="auto">
  <name>Task 2: Optimize spacing and padding throughout all sections</name>
  <files>src/components/about-section.tsx, src/components/contact-section.tsx, src/components/experience-timeline.tsx, src/components/skills-section.tsx, src/components/projects-section.tsx</files>
  <action>Apply generous, Apple-inspired spacing throughout:

1. **Update section spacing** - Add to each section component:
```tsx
// Hero section - already has min-h-screen

// About section
<section id="about" className="py-24 md:py-32">
  {/* ... content */}
</section>

// Experience section
<section id="experience" className="py-24 md:py-32 bg-muted/30">
  {/* ... content */}
</section>

// Skills section
<section id="skills" className="py-24 md:py-32">
  {/* ... content */}
</section>

// Projects section
<section id="projects" className="py-24 md:py-32 bg-muted/30">
  {/* ... content */}
</section>

// Contact section
<section id="contact" className="py-24 md:py-32">
  {/* ... content */}
</section>
```

2. **Optimize container spacing** - Ensure consistent max-width and padding:
```tsx
<div className="container mx-auto px-4 md:px-6 lg:px-8 max-w-7xl">
  {/* Section content */}
</div>
```

3. **Refine card padding** in all components:
- Timeline cards: p-6 md:p-8
- Skill cards: p-6 md:p-7
- Project cards: p-7 md:p-8
- Feature cards: p-5 md:p-6

4. **Add breathing room** between elements:
- Space between sections: py-24 to py-32 (96-128px)
- Space between cards: gap-6 to gap-8 (24-32px)
- Space between heading and content: mb-8 to mb-12
- Space within cards: space-y-4 to space-y-6

5. **Update contact section** in src/components/contact-section.tsx:
```tsx
<div className="grid md:grid-cols-2 gap-12 md:gap-16 max-w-5xl mx-auto">
  {/* Left column */}
  <div className="space-y-8">
    <h2 className="text-hero font-semibold tracking-tight">
      Get in Touch
    </h2>
    <p className="text-lg leading-relaxed text-muted-foreground">
      {/* ... */}
    </p>
  </div>

  {/* Right column */}
  <div className="space-y-6">
    {/* Social links with generous spacing */}
  </div>
</div>
```

DO NOT:
- Don't make spacing too large (maintains focus)
- Don't be inconsistent (use 8px grid system)
- Don't break mobile layout with too much padding
</action>
  <verify>All sections have py-24 md:py-32 spacing, cards have generous padding, gaps are consistent (multiples of 4 or 8), mobile layout not compromised</verify>
  <done>Spacing optimized with Apple-inspired generosity, consistent 8px grid system, responsive padding, and breathing room throughout</done>
</task>

<task type="auto">
  <name>Task 3: Apply final visual polish and consistency checks</name>
  <files>src/components/footer.tsx, src/index.css, src/components/navigation.tsx</files>
  <action>Complete the Apple-inspired transformation with final polish:

1. **Refine navigation typography** in src/components/navigation.tsx:
```tsx
// Nav links with refined typography
{navItems.map((item) => (
  <a
    key={item.name}
    href={item.href}
    className="text-sm font-medium text-foreground/80 hover:text-foreground transition-colors tracking-wide"
  >
    {item.name}
  </a>
))}
```

2. **Enhance footer** in src/components/footer.tsx:
```tsx
<footer className="border-t border-border/50 bg-muted/20 backdrop-blur-sm py-12 md:py-16">
  <div className="container mx-auto px-4 md:px-6 max-w-7xl">
    <div className="flex flex-col md:flex-row justify-between items-center gap-6">
      {/* Copyright with refined typography */}
      <p className="text-sm text-muted-foreground tracking-wide">
        © {new Date().getFullYear()} Rohan Khurana. Crafted with React + TypeScript.
      </p>

      {/* Social links with consistent spacing */}
      <div className="flex items-center gap-6">
        {/* ... social links with uniform spacing */}
      </div>
    </div>
  </div>
</footer>
```

3. **Add smooth scroll behavior** globally:
```css
/* Add to src/index.css */
@layer base {
  html {
    scroll-behavior: smooth;
  }

  /* Optimize focus states for keyboard navigation */
  :focus-visible {
    outline: 2px solid oklch(0.55 0.12 250 / 0.5);
    outline-offset: 2px;
  }

  /* Remove default focus outline for mouse users */
  :focus:not(:focus-visible) {
    outline: none;
  }
}
```

4. **Add loading state optimization** - Update section loader:
```tsx
// In App.tsx, enhance SectionLoader
function SectionLoader() {
  return (
    <div className="flex items-center justify-center py-24">
      <div className="flex gap-3">
        <div className="w-3 h-3 bg-primary rounded-full animate-bounce [animation-delay:-0.3s]" />
        <div className="w-3 h-3 bg-primary rounded-full animate-bounce [animation-delay:-0.15s]" />
        <div className="w-3 h-3 bg-primary rounded-full animate-bounce" />
      </div>
    </div>
  );
}
```

5. **Add final polish utilities**:
```css
@layer utilities {
  /* Smooth transitions for all interactive elements */
  .transition-smooth {
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Subtle fade-in animation */
  .fade-in {
    animation: fadeIn 0.6s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Optimize images for crisp rendering */
  img {
    image-rendering: -webkit-optimize-contrast;
  }
}
```

6. **Add accessibility enhancements**:
```css
/* Ensure minimum touch target sizes */
@media (pointer: coarse) {
  button, a {
    min-height: 44px;
    min-width: 44px;
  }
}

/* Optimize text rendering */
.text-optimized {
  text-rendering: optimizeLegibility;
}
```

DO NOT:
- Don't skip any sections for polish
- Don't compromise accessibility for aesthetics
- Don't break existing functionality
</action>
  <verify>All sections have consistent typography, spacing is uniform, transitions are smooth, focus states work, accessibility is maintained</verify>
  <done>Final visual polish applied, typography refined throughout, spacing consistent, accessibility enhanced, Apple-level detail achieved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Typography uses display/hero/section scales consistently
- [ ] All sections have py-24 md:py-32 spacing
- [ ] Card padding is generous (p-6 to p-8)
- [ ] Focus states are visible and accessible
- [ ] Smooth scroll behavior works
- [ ] Loading states are polished
- [ ] No visual inconsistencies remain
- [ ] Works in both light and dark modes
</verification>

<success_criteria>

- Typography refined with Apple-level sophistication
- Consistent spacing throughout (8px grid system)
- Generous padding and breathing room
- Smooth transitions and animations
- Accessibility maintained (focus states, touch targets)
- Cross-browser consistency
- Complete visual polish across all sections
- Phase 11 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-advanced-design-enhancement/11-05-SUMMARY.md`:

# Phase 11 Plan 5: Typography & Polish Summary

**Completed Apple-inspired transformation with refined typography, optimized spacing, and meticulous visual polish across the entire portfolio.**

## Accomplishments

- Applied display/hero/section typography scales throughout
- Optimized spacing with generous, Apple-inspired padding
- Enhanced navigation and footer with refined typography
- Added smooth scroll and optimized transitions
- Implemented accessibility enhancements
- Achieved Apple-level visual polish and consistency

## Files Created/Modified

- `src/index.css` - Typography utilities, smooth scroll, accessibility
- `src/components/hero.tsx` - Refined typography with display/hero scales
- `src/components/about-section.tsx` - Optimized spacing (py-24 md:py-32)
- `src/components/contact-section.tsx` - Enhanced typography and spacing
- `src/components/experience-timeline.tsx` - Consistent spacing
- `src/components/skills-section.tsx` - Optimized card padding
- `src/components/projects-section.tsx` - Generous spacing throughout
- `src/components/navigation.tsx` - Refined link typography
- `src/components/footer.tsx` - Enhanced layout and typography

## Decisions Made

- Display scale for hero name (48-96px responsive)
- Hero scale for title and section headings (40-80px responsive)
- Section scale for subheadings (32-48px responsive)
- Py-24 md:py-32 for all sections (96-128px vertical spacing)
- Tracking-tight on all headings for premium feel
- 400ms cubic-bezier transitions for smooth feel
- Smooth scroll behavior globally

## Issues Encountered

None - final polish completed successfully

## Deviations from Plan

None - all planned refinements implemented

## Next Phase Readiness

**Phase 11 Complete: Advanced Design Enhancement**

The portfolio has been transformed from basic-to-intermediate design to Apple-inspired, premium aesthetics. All content and routes remain unchanged, while the visual design now features:

✅ Premium mesh gradient backgrounds with grain textures
✅ Bento grid layouts with asymmetric cards
✅ Sophisticated glassmorphism and depth effects
✅ Refined typography with Apple-level sophistication
✅ Generous spacing and visual breathing room
✅ Smooth animations and transitions
✅ Complete visual polish and consistency

**Ready for:** Phase 10 remaining plans (10-02, 10-03, 10-04) to complete responsive optimization, performance, and final polish.
</output>
