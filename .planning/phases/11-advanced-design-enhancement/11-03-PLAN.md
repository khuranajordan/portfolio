---
phase: 11-advanced-design-enhancement
plan: 03
type: execute
domain: ui-design
---

<objective>
Transform sections into Apple-inspired bento grid layouts with asymmetric, size-varied cards.

Restructure the skills and projects sections with bento grid layouts that create visual interest and hierarchy through asymmetric card sizing and spacing.

Purpose: Implement Apple's signature bento grid aesthetic to organize content in a visually striking, premium way while maintaining readability and responsiveness.

Output: Bento grid layouts for skills section, projects section, and enhanced hero section with Apple-style asymmetric design.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-advanced-design-enhancement/DESIGN-SYSTEM.md
@.planning/phases/11-advanced-design-enhancement/11-01-SUMMARY.md
@.planning/phases/11-advanced-design-enhancement/11-02-SUMMARY.md
@src/components/skills-section.tsx
@src/components/projects-section.tsx
@src/components/hero.tsx
@src/components/project-card.tsx

**Tech stack available:**
- Tailwind CSS v4 grid system
- Framer Motion for grid animations
- Existing component structure

**Established patterns from prior plans:**
- Glassmorphism utility class available
- Enhanced spacing scale from design system
- Mesh gradient backgrounds working

**Current layouts:**
- Skills: Responsive grid with uniform cards (2-5 columns)
- Projects: Grid with showcase + cards (2-3 columns)
- Hero: Centered text with CTAs

**Apple bento grid patterns:**
- Asymmetric card sizes (some span 2 cols, some 2 rows)
- Varied spacing between cards
- Some cards are featured (larger, prominent)
- Clear visual hierarchy through size variation
- Generous padding within cards
- Seamless grid gaps (4-8px)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transform skills section into bento grid layout</name>
  <files>src/components/skills-section.tsx</files>
  <action>Restructure skills section with Apple-inspired bento grid:

1. **Update grid layout** to use asymmetric sizing:
```tsx
// Find the skills grid div and update
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6">
  {filteredSkills.map((skill, index) => (
    <motion.div
      key={skill.name}
      // Featured cards span more space
      className={cn(
        "glass-card rounded-2xl p-6 hover:shadow-xl transition-all duration-300",
        // Make some cards span 2 columns for variety
        (index === 0 || index === 5) && "md:col-span-2",
        // Make some cards span 2 rows
        (index === 2) && "md:row-span-2"
      )}
      // ... rest of component
    />
  ))}
</div>
```

2. **Add featured styling** for larger cards:
```tsx
// For featured (spanning) cards, add more content
{index === 0 && (
  <div className="mt-4 pt-4 border-t border-border/50">
    <p className="text-sm text-muted-foreground">
      Most used technologies across all projects
    </p>
  </div>
)}
```

3. **Increase card padding** for more generous spacing:
- Change from p-4/p-6 to p-6/p-8
- Increase gap from gap-3 to gap-4 or gap-6

4. **Add stagger animation** to grid layout:
```tsx
import { motion } from "framer-motion";

const container = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: {
      staggerChildren: 0.1
    }
  }
};

const item = {
  hidden: { opacity: 0, scale: 0.9 },
  show: { opacity: 1, scale: 1 }
};

// Use in component
<motion.div
  variants={container}
  initial="hidden"
  animate="show"
  className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6"
>
  {filteredSkills.map((skill, index) => (
    <motion.div key={skill.name} variants={item} className="...">
      {/* ... */}
    </motion.div>
  ))}
</motion.div>
```

DO NOT:
- Don't break mobile layout (keep 2 cols on mobile)
- Don't overdo the spanning (max 20-30% of cards)
- Don't compromise content readability
</action>
  <verify>Grid loads without errors, cards have varied sizes, layout is responsive (mobile/tablet/desktop), animations work smoothly</verify>
  <done>Skills section transformed to bento grid with asymmetric cards, featured cards span multiple rows/cols, stagger animation added</done>
</task>

<task type="auto">
  <name>Task 2: Transform projects section with bento grid layout</name>
  <files>src/components/projects-section.tsx</files>
  <action>Restructure projects section with bento grid for project cards:

1. **Update projects grid** to use asymmetric bento layout:
```tsx
// Find the projects grid (not showcase projects) and update
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  {cardProjects.map((project, index) => (
    <motion.div
      key={project.name}
      className={cn(
        "glass-card rounded-3xl overflow-hidden hover:shadow-2xl transition-all duration-500",
        // Featured project cards span 2 cols
        index < 2 && "lg:col-span-2"
      )}
      // ... rest of component
    />
  ))}
</div>
```

2. **Enhance showcase projects** with larger, more prominent layout:
```tsx
// For showcase projects (E-commerce, Task Management), add:
<div className="glass-card rounded-3xl overflow-hidden hover:shadow-2xl transition-all duration-500">
  {/* Add featured badge */}
  <div className="bg-primary/10 text-primary px-4 py-1 text-sm font-medium rounded-full inline-block mb-4">
    Featured Project
  </div>

  {/* ... rest of showcase content */}
</div>
```

3. **Add card entrance animations** with stagger:
```tsx
const container = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: {
      staggerChildren: 0.15
    }
  }
};

const item = {
  hidden: { opacity: 0, y: 20 },
  show: { opacity: 1, y: 0 }
};
```

4. **Increase spacing** for more generous feel:
- Card gaps: gap-6 to gap-8 on large screens
- Card padding: p-6 to p-8
- Section padding: py-20 to py-24 or py-32

5. **Add hover effects** to bento cards:
```tsx
whileHover={{
  y: -8,
  scale: 1.02,
  transition: { duration: 0.3 }
}}
```

DO NOT:
- Don't break showcase projects (they remain full-width)
- Don't create awkward gaps in grid
- Don't compromise project information visibility
</action>
  <verify>Projects grid has asymmetric layout, featured projects span multiple columns, animations work smoothly, mobile layout is intact</verify>
  <done>Projects section transformed with bento grid, featured cards span 2 cols, enhanced hover effects, stagger animations added</done>
</task>

<task type="auto">
  <name>Task 3: Enhance hero section with Apple-style asymmetric layout</name>
  <files>src/components/hero.tsx</files>
  <action>Update hero section with Apple-inspired asymmetric elements:

1. **Add floating info cards** to hero (Apple-style feature highlights):
```tsx
// Add after the main hero content, before scroll indicator
<motion.div
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.8, delay: 0.8 }}
  className="absolute bottom-32 left-4 md:left-8 lg:left-16 hidden md:block"
>
  <div className="glass-card rounded-2xl p-4 max-w-xs">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-xl bg-primary/10 flex items-center justify-center">
        <Sparkles className="w-5 h-5 text-primary" />
      </div>
      <div>
        <p className="text-sm font-semibold">3+ Years Experience</p>
        <p className="text-xs text-muted-foreground">Senior Frontend Developer</p>
      </div>
    </div>
  </div>
</motion.div>

// Add another card on the right
<motion.div
  initial={{ opacity: 0, y: 30 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.8, delay: 1 }}
  className="absolute bottom-32 right-4 md:right-8 lg:right-16 hidden lg:block"
>
  <div className="glass-card rounded-2xl p-4 max-w-xs">
    <div className="flex items-center gap-3">
      <div className="w-10 h-10 rounded-xl bg-primary/10 flex items-center justify-center">
        <Code2 className="w-5 h-5 text-primary" />
      </div>
      <div>
        <p className="text-sm font-semibold">React + TypeScript</p>
        <p className="text-xs text-muted-foreground">Modern Tech Stack</p>
      </div>
    </div>
  </div>
</motion.div>
```

2. **Update hero text** with larger, more prominent typography:
```tsx
<h1 className="text-display font-bold mb-6 md:mb-8 tracking-tight">
  {/* ... name */}
</h1>

<h2 className="text-hero font-semibold text-primary mb-6">
  {/* ... title */}
</h2>
```

3. **Add subtle animated border** to hero cards:
```tsx
// Update the floating cards with animated border
<motion.div
  animate={{
    borderColor: [
      "oklch(1 0 0 / 0.1)",
      "oklch(0.55 0.12 250 / 0.3)",
      "oklch(1 0 0 / 0.1)"
    ]
  }}
  transition={{ duration: 4, repeat: Infinity, ease: "easeInOut" }}
  className="glass-card rounded-2xl p-4 border-2"
>
  {/* ... card content */}
</motion.div>
```

4. **Increase section spacing** for more breathing room:
- Container py-20 to py-24 or py-32
- Max-width constraint: max-w-6xl for tighter focus

DO NOT:
- Don't cover the CTA buttons
- Don't interfere with scroll indicator
- Don't add too many cards (max 2-3)
- Don't break on smaller screens (hidden on mobile/tablet)
</action>
  <verify>Hero has floating info cards, cards don't overlap CTAs, animations work smoothly, desktop-only cards hide correctly on mobile</verify>
  <done>Hero enhanced with floating feature cards, Apple-style asymmetric layout, animated borders, larger typography with display/hero scales</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Skills section has bento grid with varied card sizes
- [ ] Projects section has bento grid with featured cards
- [ ] Hero has floating info cards on desktop
- [ ] All animations are smooth (60fps)
- [ ] Layouts are responsive (mobile, tablet, desktop)
- [ ] Glassmorphism effect applied to cards
- [ ] No content is hidden or compromised
</verification>

<success_criteria>

- Bento grid layouts implemented in skills and projects
- Asymmetric card sizing for visual interest
- Featured cards span multiple rows/columns
- Hero section has floating feature cards
- Stagger animations for grid entries
- Responsive on all screen sizes
- Glassmorphism applied to all cards
</success_criteria>

<output>
After completion, create `.planning/phases/11-advanced-design-enhancement/11-03-SUMMARY.md`:

# Phase 11 Plan 3: Bento Grid Layouts Summary

**Transformed sections into Apple-inspired bento grid layouts with asymmetric cards, featured sizing, and stagger animations.**

## Accomplishments

- Skills section restructured with bento grid and featured cards
- Projects section enhanced with asymmetric bento layout
- Hero section updated with floating feature cards
- Stagger animations added to all grid layouts
- Glassmorphism applied throughout

## Files Created/Modified

- `src/components/skills-section.tsx` - Bento grid layout, featured cards, stagger animations
- `src/components/projects-section.tsx` - Asymmetric grid, featured project styling
- `src/components/hero.tsx` - Floating info cards, larger typography

## Decisions Made

- Featured cards span 2 columns for emphasis (skills and projects)
- 0.15s stagger delay for grid animations
- Glass cards with rounded-2xl and rounded-3xl for premium feel
- Hero cards hidden on mobile/tablet, visible on lg breakpoint
- 150ms stagger for natural grid reveal

## Next Step

Ready for 11-04-PLAN.md - Glassmorphism and depth effects
</output>
